// Generated by Melange

import HandCardCss from "./HandCard.css";
import * as Caml_option from "melange.js/caml_option.js";
import * as Css from "../../../css/Css.js";
import * as JsxRuntime from "react/jsx-runtime";

const css = HandCardCss;

function HandCard(Props) {
  let label = Props.label;
  let classNameOpt = Props.className;
  let isSelectedOpt = Props.isSelected;
  let onClick = Props.onClick;
  const className = classNameOpt !== undefined ? classNameOpt : "";
  const isSelected = isSelectedOpt !== undefined ? isSelectedOpt : false;
  const selectedClass = isSelected ? "is-selected" : "";
  let tmp = {
    children: label,
    className: Css.merge([
      "HandCard",
      selectedClass,
      className
    ])
  };
  if (onClick !== undefined) {
    tmp.onClick = Caml_option.valFromOption(onClick);
  }
  return JsxRuntime.jsx("button", tmp);
}

const make = HandCard;

export {
  css,
  make,
}
/* css Not a pure module */
