// Generated by Melange

import InputLabelCss from "./InputLabel.css";
import * as Caml_option from "melange.js/caml_option.js";
import * as Css from "../../../css/Css.js";
import * as JsxRuntime from "react/jsx-runtime";

const css = InputLabelCss;

function InputLabel(Props) {
  let classNameOpt = Props.className;
  let text = Props.text;
  let htmlFor = Props.htmlFor;
  let inputPositionOpt = Props.inputPosition;
  let childrenOpt = Props.children;
  const className = classNameOpt !== undefined ? classNameOpt : "";
  const inputPosition = inputPositionOpt !== undefined ? inputPositionOpt : "Bottom";
  const children = childrenOpt !== undefined ? Caml_option.valFromOption(childrenOpt) : null;
  const pos = inputPosition === "Top" ? "InputLabel--bottom" : (
      inputPosition === "Bottom" ? "InputLabel--top" : (
          inputPosition === "Left" ? "InputLabel--right" : (
              inputPosition === "None" ? "" : "InputLabel--left"
            )
        )
    );
  return JsxRuntime.jsxs("label", {
    children: [
      JsxRuntime.jsx("span", {
        children: text,
        className: "InputLabel-text"
      }),
      children
    ],
    className: Css.merge([
      "InputLabel",
      pos,
      className
    ]),
    htmlFor: htmlFor
  });
}

const make = InputLabel;

export {
  css,
  make,
}
/* css Not a pure module */
